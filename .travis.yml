---
sudo: required
language: python
services:
  - docker
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install python-pip
install:
  - pip install molecule
  - pip install docker
script:
  - molecule --debug test
