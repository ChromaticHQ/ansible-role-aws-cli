---
# Travis CI configuration
sudo: required
language: python
services:
  - docker
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install docker-ce
install:
  - pip install molecule
  - pip install docker
script:
  - molecule test
